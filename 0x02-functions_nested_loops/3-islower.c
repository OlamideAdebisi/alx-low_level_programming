#include "main.h"
/**
 * _islower - checks for a lower case alphabet
 * Return: return 1 if true and return 0 if false
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}

